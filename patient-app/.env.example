# Address of the backend. On a phone in the same Wi-Fi, use your laptop's LAN
# address (find it with `hostname -I`), e.g. http://192.168.1.20:8000
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000

# http talks to the backend; mock runs on fixtures with nothing running.
EXPO_PUBLIC_API_MODE=mock
