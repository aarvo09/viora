# Point at the backend. Use your laptop's LAN IP if opening the dashboard
# from another machine — find it with `hostname -I`.
VITE_API_BASE_URL=http://localhost:8000

# `http` talks to the real backend. `mock` runs the UI with fixture data
# and no backend at all — useful for design work.
VITE_API_MODE=http
